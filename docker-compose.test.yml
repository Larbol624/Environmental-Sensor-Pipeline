services:
  spark-tests:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.test-spark
    volumes:
      - ./tests/test_spark:/app/tests
      - ./src:/app/src
    working_dir: /app
    environment:
    - PYTHONPATH=/app
    command:
      bash -c "pytest tests/ -vv"
    